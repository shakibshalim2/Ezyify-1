// Committed counterpart of the git-ignored `expo-env.d.ts` (generated only by `expo start`), so CI typechecks
// against the same Expo/react-native-web augmentations (e.g. Pressable `hovered`) as a local dev machine.
/// <reference types="expo/types" />
export {};
