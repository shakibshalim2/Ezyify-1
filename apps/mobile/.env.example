# Copy to .env (git-ignored). EXPO_PUBLIC_* values are inlined at build time.
EXPO_PUBLIC_API_BASE_URL=https://api.ezyify.app/v1
# live (default) | mock — mock runs the in-process demo API from @ezyify/core/mock (no backend required)
EXPO_PUBLIC_API_MODE=live
